
# before-it-disappears

A service seems to have crashed unexpectedly, leaving behind a dump and a binary.
Could you reconstruct what the program was doing?

Artifacts:
`core.12003`
`systemd-resolved`

Hint 1:
how can you analyze both the core dump and the binary?

Hint 2:
load the dump and the binary into a debugger.

Hint 3:
start by digging through the variables and pointers.

Solution in `../solution/`
