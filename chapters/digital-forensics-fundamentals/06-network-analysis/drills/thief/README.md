
# thief

Oh no, something was stolen.

Artifacts: 
`thief.pcap`

1. Who stole it?

The flag format is `SSS{IP}`

2. How many bytes did the victim send to the attacker?

The flag format is `SSS{numberofbytes}`

3. What language does the attacker presumably use?

The flag format is `SSS{language}`

Hint 1:
what is the IP of the attacker?

Hint 2:
how do you calculate the number of bytes sent?

Hint 3:
a comment might reveal the attacker's language.

Solution in `../solution/`
